# 建置過程紀錄（交接筆記）

這份文件記錄這個專案第一次建置時的過程、決策脈絡與現況，方便之後回來繼續處理時
快速抓到「為什麼是現在這樣」，不用重新問一次案主。

## 背景需求

教會活動要辦一個「價值觀競標遊戲」：
- 三段主題（生活價值 / 身心價值 / 與主的價值），每段 7 題，共 21 題
- 玩家自己輸入數字喊價（單位：萬），系統自動跑「第一次(3秒)／第二次(+7秒)／第三次成交(+15秒)」，
  中途有人加價就重新計時
- 每人籌碼 365 萬
- 現場約 30 人一起玩，1 位管理者可以選題、控場
- 遊戲結束後銜接教會信息的 PPT 分享

## 部署方式的決策過程

1. **一開始考慮本機 WiFi 架設**（案主的筆電當伺服器），優點是完全不用網路、免費、無帳號，
   但案主選擇了「免費雲端平台」，希望不限於現場網路。
2. 原本計畫用 Render.com 免費方案，但 Render 需要程式碼放在 GitHub 才能自動部署——
   **案主一開始不熟悉/不想碰 GitHub**，一度想過改回本機方案，或改用 Glitch（可以在網頁編輯器
   貼程式碼，不用 GitHub，但需要自己申請 Glitch 帳號、手動貼程式碼段落，較繁瑣）。
3. 最後案主同意「放 GitHub」，但條件是 **AI agent 代為執行所有 git 操作**，案主本人不用學指令。
   實際做法：
   - 用 `winget install --id GitHub.cli` 安裝 GitHub CLI（`gh`）
   - 用 `gh auth login --web` 走裝置授權流程，案主只需要在瀏覽器輸入一次性代碼、點 Authorize
   - AI agent 用 `git init/add/commit` + `gh repo create --push` 建立 repo 並推送，
     全程案主不用打任何 git 指令
   - 案主自己在 Render 網站用同一個 GitHub 帳號登入、選 repo、填 `ADMIN_PASSWORD`、按 Deploy
     （這一步無法由 AI agent 代勞，因為需要案主本人的 Render 帳號授權）

## 第一版狀態（喊價競標版，已淘汰）

- 已部署上線、實測確認喊價驗證、3/7/15秒計時流程、得標扣款、管理者編輯題目文字、
  結算畫面皆正常運作。
- 21 題當時是**佔位範例文字**，案主還沒提供正式內容。
- 這個版本後來被下面的「大改版」完全取代，喊價/計時機制已不存在於現在的程式碼裡。

## 大改版：案主提供實際簡報後（2026-09-07）

案主上傳了實際主日分享的簡報 `主日分享0906.pdf`，內容跟原本假設的「21題喊價競標」
完全不同：

- 是「**價值大拍賣**」，每個項目**固定價格**，不是喊價競標
- PDF 裡只有 11 個項目（4+5+2，不是 21 題），且簡報封面寫的是 100萬基金、每項也有
  自己的起標價，但案主指定：**維持 365 萬籌碼，11 個項目改用案主重新指定的價格**
  （跟 PDF 印刷的價格不同，例如 iPhone PDF 上印 8萬，實際遊戲用 9萬）
- 玩法從「喊價競標、得標者才扣款」改成「**固定價格，玩家自己決定買或不買，同一項目
  可以很多人買**」
- 遊戲結束畫面要接到 PDF 第16頁的「無法競標」（神的愛/救恩/神兒女的身份），不是原本
  想像的得標結果總覽

因此把喊價計時機制整個拆掉重寫（詳見 `agent.md` 的技術架構說明），`data/items.json`
換成案主指定的 11 項真實內容與價格。同時應案主要求，用 PyMuPDF 把 PDF 每一頁抽成
JPEG 圖片放進 `public/images/`，接到投影頁面當背景（模糊處理，避免圖片上印的舊價格
跟畫面上顯示的新價格互相打架——這點在 `agent.md` 裡有詳細說明，之後改價格不用去動圖片）。

改版後同樣寫了 `socket.io-client` 測試腳本驗證：開放前不能買、餘額不足擋下、
重複購買擋下、管理者不能修改開放中的項目、結束遊戲後個人購買清單正確——全部通過才推上線。

## 第二次大改版：拿掉管理者控場（同一天，2026-09-07）

案主實際測試後回報一個「重新整理頁面會跳回輸入名字」的疑似 bug——追查後認為根因是
Render 免費方案冷啟動（閒置後喚醒要 30-60 秒），`player.html` 原本沒有「連線中」的
過渡畫面，卡在等待期間看起來就像「被要求重新輸入名字」。修了這個 UX（加了
`connectingScreen` 過渡畫面）。

修完這個之後，案主緊接著提出更根本的需求調整：**管理者不應該逐一開放項目，應該讓
每個玩家一進來就能自由瀏覽全部 11 項自己決定買什麼，管理者頂多能看到誰在玩、買了
什麼**。這代表「管理者開放項目 → 大家搶著決定買不買 → 管理者關閉換下一項」這個
（案主自己在上一版要求做的）流程整個是錯的方向，於是又拆掉重寫：

- 拿掉 `currentItemId`／「開放中項目」這個全域狀態，11 個項目永遠都可以買
- 玩家頁面從「等待管理者開放的單一項目畫面」改成「一次列出全部11項的清單，各自有
  獨立的購買按鈕」
- 管理者面板拿掉「點題目開放」的操作，改成純觀察：玩家名單 + 每人已購買的項目明細
  （不只是數量）
- 投影畫面從「一次聚焦一個項目的大螢幕reveal」改成「即時統計看板」：11項全部列出，
  旁邊即時顯示各自的購買人數，QR Code 一直留在畫面上方方便隨時掃碼加入
- 編輯項目的限制從「不能改正在開放中的項目」改成「不能改已經有人買過的項目」
  （因為沒有「開放中」這個狀態了，改成用「有沒有人買」當限制條件）

這次修改後同樣寫 `socket.io-client` 測試腳本驗證：不用管理者開放就能直接買、
管理者能看到完整購買明細、已購買項目不能再編輯、結束遊戲照常觸發——全部通過才推上線。

**這是這個專案第二次因為「猜測玩法」而做錯方向重來**：第一次是喊價競標猜錯（案主要的
是固定價格），第二次是「管理者逐一開放」猜錯（案主要的是玩家自助全開放）。之後若還要
改玩法，優先確認清楚「節奏由誰控制」這個問題，不要預設任何一種模式。

## 第三次調整：從「逐項即買」改成「先勾選、一次確定送出」（2026-09-08）

案主反映玩家頁面希望能一次截圖分享（不要有分段/下拉），並且希望購買方式從「點一項
馬上扣款」改成「先勾選多項、下方列出已選清單、按確定才一次送出，送出後鎖死不能再改」：

- `player.html`：11 個項目合併成同一張卡片（拿掉原本 3 張分段卡片的視覺分隔），
  版面加密（縮小圖片、間距）盡量減少捲動。點項目列＝勾選/取消勾選，純前端狀態，
  送出前不打伺服器。頁面底部新增固定的「已選清單＋總價＋確定送出」區塊。
- `server.js`：拿掉逐項即買的 `item:buy`，改成 `player:confirmPurchases`（帶整批
  `itemIds`），伺服器一次驗證總價 <= 餘額後原子性扣款，並把玩家標記
  `confirmed = true`。`confirmed` 為單向鎖，送出後同一玩家不能再送出第二次，
  前端也會把畫面切成唯讀。`admin:resetGame` 會重設 `confirmed`。
- `admin.html`：玩家名單多顯示「✓已送出」跟「尚未送出選擇」狀態。

注意：11 個項目在手機螢幕上要完全不捲動仍有困難（版面已盡量壓縮），這點跟案主
說明過，不是沒做到，是螢幕高度限制。

### 後續微調（同一天）

案主測試後回報兩個問題，已修正：
1. 項目清單原本是直向清單，案主要求改成並排的格子（2 欄），底部確定鈕改成
   正常排版流動（拿掉 `position: sticky`），不要浮動吸底。
2. 「我的籌碼」原本只有按下確定送出後才會更新，中途勾選/取消都不會反映——
   案主要求勾選當下就要即時扣算預覽。改法：新增 `myBalance`（伺服器權威餘額，
   只有送出成功才會變）跟 `updateBalanceDisplay()`（畫面顯示 = 未送出時
   `myBalance - 已選總價`，送出後顯示 `myBalance`），每次勾選/取消、送出成功、
   價格變動都要呼叫這個函式同步畫面。

## 目前狀態

- GitHub repo：`https://github.com/jamespassion99/church-value-auction`
- 已部署上線：`https://church-value-auction.onrender.com`
  - 玩家：`/`　管理者：`/admin`（密碼由案主自己在 Render 環境變數設定，未記錄在任何檔案）
  - 投影：`/display`
- 11 項內容與價格是**案主確認過的正式版本**，不是佔位文字。
- 玩法是**自助式**：玩家一進來就能看到全部項目自由購買，管理者只能看名單跟結束遊戲，
  不控制節奏、不逐一開放項目。
- 遊戲結束到 PPT 的銜接：「無法競標」reveal 已經做進遊戲本身（投影畫面自動顯示），
  再往後的金句(PDF第17頁)跟反思問題(第18頁)是**手動切換**到 PowerPoint 播放，
  沒有做自動跳轉，因為瀏覽器本來就無法直接開啟/控制 PowerPoint 應用程式。

## 待辦 / 尚未決定的事項

- [ ] 是否需要把金句(第17頁)、反思問題(第18頁)也做進遊戲網頁（目前案主只要求做到
      第16頁的無法競標reveal，之後的還是手動切PPT）
- [ ] 活動結束後是否需要保留/匯出購買紀錄（目前重啟伺服器就會清空，沒有做匯出功能）

## 給下一次接手的人的提醒

- 案主不熟悉技術操作，溝通時盡量給「照著點」的步驟，不要假設對方懂 git/終端機/環境變數。
- 案主明確表示不想自己碰 GitHub——之後任何程式碼變更，預設由 AI agent 直接
  commit + push（`gh` 已經授權在這台機器上），不要又要求案主自己操作 git。
- 免費方案的雲端服務會睡眠，每次重要展示/活動前記得提醒先訪問網址喚醒。
